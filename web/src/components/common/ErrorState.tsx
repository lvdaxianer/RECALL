/**
 * Recall · 通用错误态组件（再导出 shadcn 风格版本）
 *
 * v1.3 起 recall 目录下提供带 Alert + 可选重试按钮的错误态；
 * 业务模块继续从 `components/common` 引入该组件，路径稳定不变。
 *
 * @author lvdaxianerplus
 */
export { ErrorState } from "../recall/ErrorState";
