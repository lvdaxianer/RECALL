/**
 * Recall · 通用加载态组件（再导出 shadcn 风格版本）
 *
 * v1.3 起 recall 目录下提供带 Skeleton + role="status" 的加载态；
 * 业务模块继续从 `components/common` 引入该组件，路径稳定不变。
 *
 * @author lvdaxianerplus
 */
export { LoadingState } from "../recall/LoadingState";
